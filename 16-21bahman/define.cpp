#include <bits/stdc++.h>

#define for(x) for(int i = 0; i < x; i++)
#define pii pair<int, int>

using namespace std;

int main(){
      for(5){
            cout << i << endl;
      }
      pii p = make_pair(2, 4);
      cout << p.first;
      return 0;
}