#include <iostream>

using namespace std;

int main(){
      int width, length, area;
      width = 20;
      length = 40;
      area = length * width;
      cout << "Area is: " << area << endl;
      return 0;
}