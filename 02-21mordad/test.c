#include <stdio.h>

int main () {

      int length = 10, width = 9, perimeter;

      perimeter = 2 * (length + width);
      printf("perimeter is: %d\nlength is: %d\nwidth is: %d", perimeter, length, width);
      return 0;
}