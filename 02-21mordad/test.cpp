#include <iostream>

using namespace std;
int main () {

      int length = 10, width = 9, perimeter;

      perimeter = 2 * (length + width);
      cout << "perimeter is: " << perimeter << endl << "length is: " << length << endl << "width is : " << width << endl;
      return 0;
}