#include <iostream>
#include <cmath>

using namespace std;

int main () {
      double radius;
      cin >> radius;
      double area = M_PI * radius * radius;
      cout << area << endl;
      return 0;
}